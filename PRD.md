# Task Ocean — 제품 요구사항 문서 (PRD)

| 항목 | 내용 |
|---|---|
| 제품명 | Task Ocean |
| 플랫폼 | macOS 전용 데스크탑 앱 |
| 문서 버전 | v0.5 (Draft) |
| 작성일 | 2026-07-14 |
| 상태 | 검토 대기 |
| 연동 대상 | Google Tasks API (v1) |
| 배포 전략 | Developer ID 직접 배포(공증) 우선 → 이후 Mac App Store |
| 최소 지원 OS | macOS 14 Sonoma (타깃: 최신 macOS) |
| 수익화 | Freemium — 무료 단일 계정 코어 + Pro(멀티 계정·고급 기능) 일회성 구매 |

---

## 1. 개요

**Task Ocean**은 Google Tasks를 macOS 데스크탑에서 "항상 화면 위에 떠 있는 작은 창"으로 상시 노출·관리하는 경량 유틸리티다. macOS 메모(Notes)의 *항상 상단에 유지* 경험처럼, 다른 작업을 하는 동안에도 할 일 목록을 곁에 두고, 전역 키보드 숏컷으로 어디서든 즉시 할 일을 캡처할 수 있게 한다.

기존 Google Tasks는 웹(Gmail/Calendar 사이드바)과 모바일 중심이라, 데스크탑에서 "항상 보이게 두기"와 "빠른 캡처"가 어렵다. Task Ocean은 이 두 가지 공백을 메우는 데 집중하며, 여러 Google 계정(업무+개인)을 하나의 화면에서 통합 관리하는 것을 차별점으로 삼는다.

---

## 2. 배경 및 문제 정의

**문제**
- Google Tasks 데스크탑 접근은 브라우저 탭/사이드바에 종속되어 있어, 다른 앱을 쓰는 동안 할 일이 시야에서 사라진다.
- 떠오른 할 일을 즉시 적으려면 브라우저 전환 → 앱 열기 → 리스트 선택이라는 마찰이 크다.
- 업무용·개인용 등 여러 Google 계정을 쓰는 사용자는 계정을 오가며 확인해야 한다.
- macOS 네이티브 경험(메뉴바, Dock, 항상 위 창, 전역 단축키)과 결합된 Google Tasks 전용 클라이언트가 사실상 없다.

**기회**
- "항상 위 + 빠른 캡처 + 멀티 계정 통합"은 명확하고 좁은 가치 제안이며, 경량 유틸리티로 빠르게 구현·차별화할 수 있다.
- Google Tasks API는 태스크/리스트 CRUD를 안정적으로 제공하므로 자체 백엔드 없이 클라이언트만으로 완결적인 제품이 가능하다.

---

## 3. 목표와 비목표

### 3.1 목표 (Goals)
- **G1.** 할 일을 시야에서 놓치지 않게 한다 — 어떤 앱 위에서도 유지되는 항상-위 창을 제공한다.
- **G2.** 캡처 마찰을 최소화한다 — 전역 단축키로 3초 이내에 할 일을 추가할 수 있다.
- **G3.** 여러 Google 계정의 태스크를 하나의 화면에서 본다.
- **G4.** Google Tasks의 태스크/서브태스크/리스트 관리를 앱 안에서 완결적으로 처리한다.
- **G5.** 상시 실행 유틸리티로서 가볍고(낮은 메모리/CPU), 방해되지 않는다.
- **G6.** **하루 단위로 "오늘 할 일"에 집중**하는 뷰를 제공한다(넓은 기간은 히트맵으로 조망, §6.6).
- **G7.** **끊김 없는 신뢰성** — 오프라인·세션 만료 상황에서도 데이터 유실·중복 없이 매끄럽게 동작한다(§8.3, §8.7).

### 3.2 비목표 (Non-Goals)
- Windows / Linux / iOS 지원 (이번 범위는 macOS 전용).
- 자체 태스크 백엔드/동기화 서버 구축 (동기화는 Google Tasks가 소스 오브 트루스).
- 팀 협업, 공유 리스트, 코멘트 등 협업 기능.
- 프로젝트 관리 성격의 고급 기능(간트, 의존성, 보드 등).
- **달력 월/주 그리드에 태스크를 채워 보여주는 뷰는 제공하지 않는다** — 넓은 기간 조망은 히트맵으로 대체한다(§6.6).
- Google Tasks가 API로 지원하지 않는 기능의 임의 구현(§8.4 참조) — 예: 서버측 반복/리마인더.

---

## 4. 타깃 사용자 & 페르소나

**페르소나 A — "멀티태스킹 지식노동자" (주 사용자)**
Mac에서 하루 종일 여러 앱을 오가며 일한다. Google Tasks를 쓰지만 브라우저 탭에 묻혀 자주 잊는다. 회의 중 떠오른 할 일을 즉시 적고 싶다. 화면 한켠에 오늘 할 일이 늘 떠 있길 바란다.

**페르소나 B — "업무/개인 분리 사용자"**
회사 Google Workspace 계정과 개인 Gmail 계정을 함께 쓴다. 두 계정의 할 일을 왔다 갔다 확인하는 게 번거롭다. 한 화면에서 통합해 보되, 어떤 게 어느 계정인지 구분되길 원한다.

**페르소나 C — "키보드 우선 파워유저"**
마우스 사용을 최소화하고 단축키로 모든 걸 처리하려 한다. 앱을 열지 않고도 할 일을 추가/완료하고 싶어 한다.

---

## 5. 핵심 사용자 시나리오

- **S1.** 코드를 짜다 아이디어가 떠오른다 → 전역 단축키를 누른다 → 퀵 캡처 창에 "리팩터링 문서 작성"을 입력하고 Enter → 창이 사라지고 방금 만든 태스크가 항상-위 창에 뜬다.
- **S2.** 회의 화면을 공유 중이다 → 화면 한켠의 작은 항상-위 창에서 오늘 할 일을 훑고, 끝난 항목을 체크한다.
- **S3.** 업무 계정의 "이번 주" 리스트와 개인 계정의 "장보기" 리스트를 동시에 보며 하루를 계획한다.
- **S4.** 큰 태스크에 서브태스크 3개를 추가하고, 드래그로 순서를 바꾼다.
- **S5.** 창을 반투명·컴팩트 모드로 두고, Dock 아이콘의 배지로 남은 할 일 개수를 확인한다.
- **S6.** 상단 화살표로 하루하루 넘기며 계획한다 → 히트맵에서 다음 주 목요일이 유난히 진한 걸 보고 클릭 → 그날로 이동해 몰린 태스크를 앞뒤로 분산한다.
- **S7.** 아침에 앱을 여니 회사 계정에만 "재로그인 필요" 배너가 떠 있다(개인 계정은 정상) → 배너를 눌러 로그인하고 브라우저에서 앱으로 돌아오자, 보던 하루 화면과 작성 중이던 태스크 입력이 그대로 유지된다.

---

## 6. 기능 요구사항

> 우선순위: **P0** = MVP 필수, **P1** = v1 이후 빠른 후속, **P2** = 향후. 각 요구사항에 수용 기준(AC)을 함께 기재한다.

### 6.1 항상-위 창 & 창 동작 (제품 핵심)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-1.1 | 메인 창을 다른 모든 앱 위에 유지하는 "항상 상단" 토글을 제공한다. | P0 |
| FR-1.2 | 창 위치·크기를 저장하고 재실행 시 복원한다(멀티 디스플레이 포함). | P0 |
| FR-1.3 | 컴팩트/확장 뷰 모드 전환(제목만 vs 상세 포함). | P1 |
| FR-1.4 | 창 투명도(불투명도) 조절 슬라이더. | P1 |
| FR-1.5 | 화면 가장자리 스냅 / 미니 모드(가느다란 스트립). | P2 |
| FR-1.6 | 모든 스페이스(가상 데스크탑)에 표시 옵션. | P1 |
| FR-1.7 | 마우스가 없을 때 자동 페이드/반투명(방해 최소화) 옵션. | P2 |

**AC:** 항상-위를 켜면 전체화면 앱을 제외한 어떤 앱을 포커스해도 창이 가려지지 않는다. 재실행 시 마지막 위치·크기·모드가 복원된다.

### 6.2 Google Tasks 관리 (태스크/서브태스크/리스트)

**태스크 리스트(Tasklist)**

| ID | 요구사항 | API 매핑 | 우선순위 |
|---|---|---|---|
| FR-2.1 | 리스트 목록 조회 | `tasklists.list` | P0 |
| FR-2.2 | 리스트 생성 | `tasklists.insert` | P0 |
| FR-2.3 | 리스트 이름 변경 | `tasklists.patch` / `update` | P0 |
| FR-2.4 | 리스트 삭제 | `tasklists.delete` | P0 |

**태스크(Task)**

| ID | 요구사항 | API 매핑 | 우선순위 |
|---|---|---|---|
| FR-2.5 | 태스크 목록 조회(완료/미완료, 숨김 포함 옵션) | `tasks.list` | P0 |
| FR-2.6 | 태스크 추가(제목, 메모, 마감일) | `tasks.insert` | P0 |
| FR-2.7 | 태스크 수정(제목/메모/마감일) | `tasks.patch` | P0 |
| FR-2.8 | 태스크 삭제 | `tasks.delete` | P0 |
| FR-2.9 | 완료/미완료 토글 | `tasks.patch` (`status`) | P0 |
| FR-2.10 | 순서 변경(드래그 정렬) | `tasks.move` (`previous`) | P0 |
| FR-2.11 | 완료 태스크 일괄 정리 | `tasks.clear` | P1 |
| FR-2.12 | 태스크를 다른 리스트로 이동 | `tasks.move` (`destinationTasklist`) | P1 |

**서브태스크(Subtask, 1단계 중첩)**

| ID | 요구사항 | API 매핑 | 우선순위 |
|---|---|---|---|
| FR-2.13 | 서브태스크 추가(부모 지정) | `tasks.insert` 후 `tasks.move` (`parent`) | P0 |
| FR-2.14 | 서브태스크 수정/삭제/완료 | 상위 태스크와 동일 | P0 |
| FR-2.15 | 들여쓰기/내어쓰기로 서브태스크 승격·강등 | `tasks.move` (`parent`) | P1 |

> ⚠️ **제약:** Google Tasks는 서브태스크를 **1단계**만 지원한다(손자 태스크 불가). 마감일은 **날짜 단위**만 저장된다(시간 무시). 상세는 §8.4.

**AC:** 앱에서 수행한 모든 CRUD는 지연 없이 UI에 반영되고(낙관적 업데이트), Google Tasks 웹/모바일과 양방향 동기화된다.

### 6.3 멀티 Google 계정 통합

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-3.1 | 여러 Google 계정을 OAuth로 추가/제거한다. | P0 |
| FR-3.2 | 모든 계정의 리스트/태스크를 하나의 화면에서 통합 표시한다. | P0 |
| FR-3.3 | 각 태스크·리스트가 어느 계정인지 시각적으로 구분(계정별 색상/아바타/라벨). | P0 |
| FR-3.4 | 계정 또는 리스트 단위로 표시 필터링(보이기/숨기기). | P0 |
| FR-3.5 | 새 태스크 추가 시 기본 대상 계정/리스트 지정. | P0 |
| FR-3.6 | 계정별 토큰을 안전하게 저장(macOS Keychain)하고 자동 갱신. | P0 |
| FR-3.7 | 계정 재인증 필요 시 명확한 안내 및 원클릭 재로그인. | P1 |

**AC:** 2개 이상 계정 연동 시, 통합 뷰에서 모든 태스크가 계정 구분과 함께 보이며, 특정 계정만 끄면 즉시 화면에서 사라진다.

### 6.4 Dock / 메뉴바 인터페이스

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-4.1 | Dock 아이콘 클릭으로 항상-위 창을 즉시 표시/포커스한다. | P0 |
| FR-4.2 | Dock 아이콘 배지로 미완료(또는 오늘 마감) 태스크 개수를 표시한다. | P1 |
| FR-4.3 | 메뉴바(상태바) 아이콘을 제공하고, 클릭 시 팝오버로 오늘 할 일을 보여준다. | P1 |
| FR-4.4 | 메뉴바 팝오버에서 바로 태스크 추가/완료 가능. | P1 |
| FR-4.5 | Dock 숨김(메뉴바 전용) 모드 선택 옵션. | P2 |

**AC:** 창이 숨겨져 있어도 Dock/메뉴바 아이콘으로 한 번의 클릭으로 창을 불러올 수 있다.

### 6.5 전역 키보드 숏컷 & 퀵 캡처

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-5.1 | **전역 단축키**로 어느 앱에서든 퀵 캡처 창을 띄운다(Spotlight 유사). | P0 |
| FR-5.2 | 퀵 캡처에서 제목 입력 후 Enter로 즉시 저장, Esc로 취소. | P0 |
| FR-5.3 | 퀵 캡처에서 대상 계정/리스트를 키보드로 선택. | P0 |
| FR-5.4 | 전역 단축키로 항상-위 창 표시/숨김 토글. | P0 |
| FR-5.5 | 자연어 마감일 파싱(예: "보고서 제출 내일", "정산 금요일"). | P1 |
| FR-5.6 | 창 내 완전 키보드 내비게이션(↑↓ 이동, Enter 편집, ⌘Enter 완료, Tab 들여쓰기 등). | P1 |
| FR-5.7 | 단축키를 사용자가 자유롭게 재설정. | P1 |

**AC:** 다른 앱이 포커스된 상태에서 전역 단축키 한 번으로 퀵 캡처가 뜨고, 입력→Enter까지 마우스 없이 3초 내 태스크가 저장된다.

### 6.6 하루 중심 뷰 & 내비게이션 (제품 핵심)

주 화면은 **"하루"**다. 달력 그리드에 태스크를 채우는 뷰는 제공하지 않고(§3.2), 상단 좌우 화살표로 하루 단위로만 이동한다. 넓은 기간 조망은 **히트맵**으로 대체한다.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-DAY-1 | 주 화면은 선택된 **하루**의 태스크를 표시한다(모든 계정·리스트 통합). | P0 |
| FR-DAY-2 | 상단 좌/우 화살표(및 단축키)로 전일·익일 이동. "오늘로" 바로가기 제공. | P0 |
| FR-DAY-3 | 태스크는 **마감일(`due`) 기준**으로 해당 날짜에 배치한다(§8.4 날짜 단위 제약). | P0 |
| FR-DAY-4 | **기한 없음(Inbox)** 태스크는 하루 뷰 하단 전용 섹션에 모아 상시 노출한다(비어있지 않으면 기본 펼침). | P0 |
| FR-DAY-5 | 지난 날짜의 미완료 태스크는 **'오늘' 뷰 상단 '지남(Overdue)' 그룹에 비파괴적으로** 표시한다(원래 `due` 유지). | P0 |
| FR-DAY-6 | **히트맵 UI**: 기본 **월 그리드**로 날짜별 밀도를 색 농도로 표시(3개월 범위 토글 제공). | P1 |
| FR-DAY-7 | 히트맵 날짜 셀을 **클릭하면 해당 하루로 이동**한다. | P1 |
| FR-DAY-8 | 히트맵 밀도 기준 = **해당일 미완료 태스크 수**(부하/주의). '완료 기준' 뷰는 향후 옵션. | P1 |
| FR-DAY-9 | 히트맵의 **주 시작 요일**은 로케일을 따른다(일/월). → §6.9 연동. | P1 |

> ⚠️ 달력 월/주 그리드에 태스크 목록을 채우는 뷰는 **비목표**(§3.2). 넓은 기간은 히트맵으로만 조망한다.

**확정 정책 (결정됨)**
- **하루 뷰 구성:** `[지남(Overdue) 그룹]` → `[당일 태스크]` → `[기한 없음(Inbox) 섹션]` 순으로 한 화면에 표시.
- **정렬:** 계정 → 리스트 → Google `position`(수동 순서 존중). **완료 항목은 각 그룹 하단**으로 내리거나 토글로 숨김. 기본은 **계정 색으로 구분한 병합 리스트**이며 리스트별 그룹핑 뷰는 옵션(P1).
- **지난 미완료 이월:** 데이터를 바꾸지 않고 '오늘'에만 오버레이 표시. 항목별 **"오늘로 옮기기"**를 누르면 그때 `due`를 오늘로 변경(사용자 명시 동작). 오버듀는 미래 날짜에는 중복 노출하지 않는다.
- **퀵 캡처 기본값:** 날짜 없이 **기한 없음(Inbox)** 에 저장(오늘을 가짜 마감으로 오염시키지 않음). NLP로 날짜가 파싱되면 해당일에 배치. (오늘 기본 저장은 설정으로 선택 가능.)

**AC:** 좌우 이동만으로 하루하루를 넘기며 관리하고, 히트맵에서 바쁜 날을 파악해 클릭으로 이동할 수 있다. 기한 없는 태스크(Inbox)와 지난 미완료(Overdue)가 유실 없이 항상 보인다.

### 6.7 동기화 무결성 & 오프라인

로컬 변경은 즉시 UI에 반영(낙관적)하고, 내구성 있는 큐로 서버에 전달하며, 폴링으로 서버 상태를 로컬에 병합한다. 중복·유실·순서 꼬임이 없도록 무결성을 보장한다. 상세 설계는 **§8.3**.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-SYNC-1 | 모든 쓰기(추가/수정/삭제/완료/이동)를 **낙관적 UI**로 즉시 반영한다. | P0 |
| FR-SYNC-2 | 변경을 **내구성 있는 작업 큐(outbox)** 에 기록해 앱 재시작·크래시 후에도 유실되지 않게 한다. | P0 |
| FR-SYNC-3 | 전송 실패 시 **지수 백오프 재시도**, 영구 실패 시 **롤백 + 비침습적 오류 표시**. | P0 |
| FR-SYNC-4 | 네트워크 재연결 시 큐를 자동 플러시한다(오프라인 지원). | P0 |
| FR-SYNC-5 | **중복 방지(idempotency)** — 재시도로 인한 태스크 중복 생성을 막는다(§8.3 재조정 전략). | P0 |
| FR-SYNC-6 | **폴링**으로 서버 변경·삭제를 로컬에 증분 반영한다(`updatedMin`, `showDeleted`). | P0 |
| FR-SYNC-7 | **충돌 해결 정책**을 적용한다(기본 최종수정우선, 가능 시 etag 조건부). | P0 |
| FR-SYNC-8 | 각 항목의 **동기화 상태**(대기/충돌/오류)를 은은하게 시각화하고 수동 재시도·새로고침을 제공한다. | P1 |

**AC:** 오프라인에서 만든 변경이 온라인 복귀 후 중복 없이 반영되고, 앱을 강제 종료해도 대기 변경이 살아남아 재동기화된다. 서버에서 바뀐 내용이 폴링 주기 내에 로컬에 반영된다.

### 6.8 멀티 계정 세션·인증 무결성

멀티 계정 환경에서 세션 만료를 매끄럽게 처리한다. 대부분은 리프레시로 조용히 갱신하고, 하드 만료 시 **해당 계정만 격리**해 재로그인으로 유도하되 나머지 계정은 계속 동작한다. 상세는 **§8.7**.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-AUTH-1 | 액세스 토큰 만료는 **리프레시 토큰으로 사전·자동 갱신**(사용자 개입 없음). | P0 |
| FR-AUTH-2 | 리프레시 실패(하드 만료·권한 철회)를 **감지**하고 해당 계정을 "재로그인 필요" 상태로 전환. | P0 |
| FR-AUTH-3 | **부분 만료 격리** — 일부 계정만 만료돼도 나머지 계정 기능과 앱 전체는 정상 유지한다. | P0 |
| FR-AUTH-4 | 만료 계정의 태스크를 **숨기지 않고** "재로그인 필요"로 명확히 표시한다(무음 소실 금지). | P0 |
| FR-AUTH-5 | **비차단 재인증 안내** — 전역 모달이 아닌 해당 계정 배너/배지로 안내하고 원클릭으로 로그인 시작. | P0 |
| FR-AUTH-6 | 재로그인(브라우저 OAuth, URL 스킴 복귀) 완료 후 **직전 상태 복원**(같은 하루 뷰·스크롤·작성 중 입력 유지). | P0 |
| FR-AUTH-7 | 만료 중 해당 계정에 가한 변경은 큐에 보존되어 **재인증 후 플러시**된다(§6.7 연동). | P1 |

**AC:** 세 계정 중 하나의 토큰이 철회돼도 앱은 멈추지 않고 그 계정만 "재로그인 필요"로 표시된다. 배너를 눌러 로그인하면 브라우저를 거쳐 돌아온 뒤 하던 화면과 입력이 그대로 유지된다.

### 6.9 다국어 지원 (i18n)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-I18N-1 | UI 전체를 **한국어·영어**로 현지화한다. | P0 |
| FR-I18N-2 | 기본 언어는 **macOS 시스템 언어**를 따르고, 앱 내 수동 전환 옵션을 제공한다. | P1 |
| FR-I18N-3 | **로케일 인지 날짜/숫자 포맷** 및 상대 표현("오늘/내일" ↔ "Today/Tomorrow"). | P0 |
| FR-I18N-4 | **주 시작 요일**(일/월)을 로케일에 맞춰 하루 뷰·히트맵에 반영한다(§6.6). | P1 |
| FR-I18N-5 | 자연어 마감일 파싱을 **양 언어** 지원한다("내일"·"금요일" / "tomorrow"·"Friday", §6.5 FR-5.5). | P1 |
| FR-I18N-6 | 문자열 길이 차이·복수형을 고려한 레이아웃(번역 확장 대응). | P1 |

> 사용자 콘텐츠(태스크 제목/메모)는 현지화 대상이 아니며, 앱 UI 텍스트만 번역한다. 구현은 String Catalog(`.xcstrings`)/`NSLocalizedString` 기반.

**AC:** 시스템 언어가 한국어면 한국어로, 영어면 영어로 표시되고, 날짜·요일·상대표현이 로케일에 맞게 나온다.

### 6.10 추가 제안 기능 (선택)

> 아래는 핵심 가치를 강화하는 제안이다. 범위 관리를 위해 대부분 P1/P2로 둔다.

| ID | 제안 | 근거 | 우선순위 |
|---|---|---|---|
| ~~FR-6.1~~ | ~~통합 스마트 뷰(오늘/예정/지남/완료)~~ → **§6.6 하루 중심 뷰로 대체·격상됨** | 뷰 모델을 하루 중심으로 확정 | — |
| FR-6.2 | **로컬 알림**: 마감일 기준 알림(사용자 지정 시각) | API가 리마인더를 지원하지 않으므로 로컬 알림으로 보완 | P1 |
| ~~FR-6.3~~ | ~~오프라인 캐시 + 변경 큐~~ → **§6.7 동기화 무결성으로 승격됨(P0)** | 상시 실행 유틸리티의 필수 신뢰성 요소 | — |
| FR-6.4 | 태스크 전체 검색(제목/메모) | 항목이 많아질 때 탐색성 | P1 |
| FR-6.5 | 다크/라이트 자동 전환, 폰트 크기 조절, 계정별 액센트 컬러 | 작은 창의 가독성·개인화 | P1 |
| FR-6.6 | 로그인 시 자동 실행(Login Items) | 상시 도구로서 필수적 편의 | P0* |
| FR-6.7 | 여러 개의 핀 창(리스트별로 화면 모서리에 고정) | 파워유저용 멀티 위젯 | P2 |
| FR-6.8 | macOS 알림센터 위젯 | 창을 열지 않고 확인 | P2 |
| FR-6.9 | macOS Focus(집중 모드) 연동 — 업무 집중 시 업무 계정만 표시 | 상황별 자동 필터 | P2 |
| FR-6.10 | 반복 태스크(앱 레벨 템플릿 생성) | API 미지원을 앱에서 우회 | P2 |

\* FR-6.6은 상시 도구 특성상 사실상 P0에 준함.

---

## 7. 비기능 요구사항 (NFR)

- **NFR-1 성능/경량성:** 상시 실행 유틸리티로서 유휴 시 메모리·CPU 점유를 최소화한다(가급적 네이티브 스택, §8.1). 유휴 메모리 목표는 실측 후 확정.
- **NFR-2 보안/프라이버시:** OAuth 토큰은 macOS Keychain에 저장한다. 최소 권한 스코프만 요청한다. 태스크 데이터는 Google과의 통신 외 외부로 전송하지 않는다(자체 서버 없음).
- **NFR-3 신뢰성/동기화:** 낙관적 업데이트 + **내구성 있는 작업 큐(outbox)** 로 앱 재시작·크래시에도 대기 변경을 보존한다. 재시도 시 **중복 생성 방지(idempotency)**, 폴링 증분 병합, 충돌 처리(기본: 최신 수정 우선, 가능 시 etag 조건부)를 보장한다(상세 §8.3).
- **NFR-4 접근성:** VoiceOver, 키보드 전용 조작, 동적 글꼴 크기를 지원한다.
- **NFR-5 호환성:** **최소 macOS 14 Sonoma**, 타깃은 최신 macOS(현재 기준 macOS 26 Tahoe 계열). `MenuBarExtra`·`SMAppService`·String Catalog 등 최신 API를 쓰기 위해 floor를 14로 두되, 초기 사용 지표에서 macOS 13 비중이 크면 하향을 재검토한다. Apple Silicon/Intel 유니버설 바이너리.
- **NFR-6 오류 처리:** 인증 만료, 쿼터 초과, 네트워크 오류를 사용자에게 명확·비침습적으로 안내한다.
- **NFR-7 배포/업데이트:** **첫 릴리스부터 App Sandbox 호환 구조로 설계**해 이후 App Store 전환 시 재작업을 최소화한다. Developer ID 빌드는 자동 업데이트(Sparkle)를 제공하고, App Store 빌드는 스토어 기본 업데이트를 사용하도록 업데이트 계층을 추상화한다(§8.6).
- **NFR-8 국제화(i18n):** 한국어·영어 현지화를 지원하고, 시스템 언어를 기본으로 따른다. 날짜·요일·상대표현·주 시작 요일 등 **로케일 인지 포맷**을 적용한다(§6.9).

---

## 8. 기술 아키텍처 & Google Tasks API 고려사항

### 8.1 기술 스택 선택지

| 옵션 | 장점 | 단점 | 적합성 |
|---|---|---|---|
| **네이티브 Swift/SwiftUI + AppKit** | 최고의 경량성·성능, 메뉴바/Dock/창 레벨/전역 핫키 등 macOS 통합 최상 | macOS 전용(의도된 범위엔 무방) | **권장** |
| Tauri (Rust + 웹뷰) | 경량, 웹 UI 재사용 | 일부 네이티브 통합은 플러그인/브리지 필요 | 대안 |
| Electron | 개발 속도 빠름, 생태계 풍부 | 메모리 무거움 — 상시 실행 유틸리티에 부적합 | 지양 |

> 제품 성격(항상 떠 있는 경량 도구)상 **네이티브 Swift** 또는 **Tauri**를 권장하고 Electron은 지양한다.

### 8.2 인증 (OAuth 2.0)
- 설치형 앱 방식으로 **PKCE**를 사용한다(클라이언트 시크릿을 앱에 임베드하지 않음).
- **리디렉션 방식:** App Store(샌드박스) 전환을 고려해 **커스텀 URL 스킴** 리디렉션을 우선한다. loopback 리디렉션(로컬 서버 수신)을 쓸 경우 샌드박스에서는 `com.apple.security.network.server` 엔타이틀먼트가 필요하므로, 재작업을 줄이기 위해 첫 릴리스부터 스킴 방식으로 통일하는 것을 권장한다.
- 스코프: 쓰기 필요 → `https://www.googleapis.com/auth/tasks` (읽기 전용 대체 스코프는 `.../tasks.readonly`). 최소 권한 원칙 적용.
- 리프레시 토큰으로 자동 갱신, 계정별 토큰은 **Keychain** 격리 저장(App Store 빌드에서는 `keychain-access-groups` 엔타이틀먼트 사용).
- **배포 유의:** Tasks 스코프는 민감 스코프로 분류되어, 외부 사용자 다수 배포 시 **Google OAuth 동의 화면 검증**이 필요할 수 있다(런칭 일정에 반영).

### 8.3 동기화 무결성 설계 (Sync Integrity)
Google Tasks가 **소스 오브 트루스**, 로컬은 캐시. 세 흐름(로컬→서버 전달 / 낙관적 UI / 서버→로컬 폴링)을 중복·유실 없이 무결하게 묶는다.

**(a) 쓰기 경로 — 낙관적 UI + 내구성 큐(outbox)**
- 사용자 동작 → 로컬 상태 **즉시 반영**(낙관적) → **디스크에 영속되는 작업 큐(outbox)** 에 변경 op 기록 → 백그라운드 워커가 API 호출 → 성공 시 서버 정본(`id`·`etag`·`updated`·`position`)으로 **재조정** → 실패 시 백오프 재시도, 영구 실패 시 **롤백 + 비침습 오류**.
- **임시 ID 매핑:** `insert` 응답 전에 부여한 로컬 UUID를 서버 발급 ID로 치환한다. 부모가 아직 생성 전인 서브태스크처럼 **의존 관계가 있는 op는 큐에서 순서를 보존**하고, 임시ID→서버ID로 참조를 해소한다.
- **엔티티별 순서 보장:** 같은 항목의 create→update→delete가 뒤섞이지 않도록 직렬화한다.

**(b) 중복 방지 (Idempotency)**
- Tasks API `insert`는 서버측 멱등 키를 제공하지 않으므로 **클라이언트가 관리**한다. 전송 결과가 불확실할 때(타임아웃 등) 재전송 전에 `updatedMin` 조회로 서버 반영 여부를 **재조정(reconcile)** 해 중복 생성을 막는다. 각 op는 in-flight로 표시하고 확인 후에만 완료 처리한다.

**(c) 읽기 경로 — 폴링 증분 병합**
- 계정·리스트별 주기 폴링. Tasks API는 웹훅/푸시 채널이 없어 폴링으로 갱신한다. `tasks.list`에 `updatedMin`(증분) + `showCompleted`·`showHidden`·`showDeleted=true`(삭제 감지)를 사용한다. 마지막 성공 동기화 시각을 저장하고 **시계 오차 여유(safety margin)** 를 둔다. (Tasks API는 Calendar류의 완전한 `syncToken`을 제공하지 않아 `updatedMin` 기반 증분으로 처리한다.)
- **폴링 주기(적응형, 확정):** 창 표시·앱 활성 시 **60초**, 백그라운드(창 숨김/비활성) **5분**, 유휴(30분+ 무조작) **15분**. 다음 이벤트에서는 **즉시 동기화**한다 — 앱 포그라운드 전환, 창 표시, 시스템 절전 해제, 네트워크 재연결, 수동 새로고침. 429/쿼터 시 백오프로 주기를 늘린다.
- **수동 새로고침(확정):** 항상 제공(⌘R 및 당김-새로고침). 전 계정 즉시 증분 동기화를 강제한다.

**(d) 충돌 해결 (확정)**
- **v1.0 기본: 필드 단위 `patch`(변경 필드만 전송) + 최종 수정 우선(LWW, `updated` 비교).** `update`(전체 교체) 대신 `patch`를 기본으로 써 사용자가 건드리지 않은 필드의 덮어쓰기(lost update)를 원천 축소한다.
- **etag 조건부(`If-Match`):** 개발 중 스파이크로 Tasks API의 `If-Match`/412 지원 여부를 검증하고, **지원이 확인되면 v1.1+에서 낙관적 동시성 제어를 추가**한다(412 시 재조회 후 병합·재적용). 미지원 시 LWW를 유지한다. 양측이 서로 다른 필드를 바꾼 경우의 자동 필드 병합은 그다음 단계.

**(e) 신뢰성**
- outbox 디스크 영속(재시작·크래시 생존), 지수 백오프+지터, 429/쿼터는 `Retry-After` 준수. 오프라인 시 전송 일시중지, 재연결 시 플러시. 시작 시 대기 큐 재생 후 증분 풀.

**(f) 멀티 계정**
- 계정별 독립 동기화 루프. 쿼터를 고려해 폴링 주기를 **적응형**으로 조절. 한 계정의 오류·만료가 다른 계정 동기화를 막지 않도록 격리한다(§8.7).

### 8.4 Google Tasks API 제약 (설계에 직접 영향) ⚠️
API 레퍼런스(§부록 A) 기준으로, 아래 제약을 UX 설계에 반드시 반영한다.

1. **마감일은 날짜 단위만 저장** — `due`는 RFC3339 형식이지만 시간 정보는 저장되지 않는다. → "몇 시" 마감/시간 알림은 API로 불가. 시간 기반 알림이 필요하면 **로컬 알림**으로 우회(FR-6.2). 또한 **하루 중심 뷰의 날짜 배치도 이 제약을 따르며**(§6.6 FR-DAY-3), 마감일이 없는 태스크는 별도 영역이 필요하다(FR-DAY-4).
2. **서브태스크 1단계만** — `parent`로 한 단계 중첩만 가능. UI에서 2단계 이상 들여쓰기를 막는다.
3. **반복(recurrence) 미지원** — API에 반복 필드 없음. 앱 레벨 템플릿으로만 우회 가능(FR-6.10).
4. **리마인더/알림 미지원** — 알림은 앱이 자체적으로 처리해야 함.
5. **계정 간 이동 불가(직접)** — 태스크 ID는 계정/리스트 스코프. 계정 간 "이동"은 대상 계정에 **재생성 + 원본 삭제**로 구현.
6. **정렬/이동은 `tasks.move`** — 같은 리스트 내 위치는 `previous`, 계층은 `parent`, 다른 리스트로는 `destinationTasklist`로 지정.
7. **완료 정리는 `tasks.clear`** — 리스트 단위 완료 태스크 일괄 삭제.
8. **푸시 없음 → 폴링** — 실시간성은 폴링 주기에 의존.
9. **사용량 제한(쿼터) 존재** — 멀티 계정·짧은 폴링 주기에서 쿼터 초과에 유의(백오프 필요).

### 8.5 macOS 권한 & 시스템 통합
- **항상-위 창:** 창 레벨 제어(플로팅 레벨). 별도 시스템 권한 불필요.
- **전역 단축키:** **`RegisterEventHotKey`(Carbon) 기반**으로 구현한다. 등록된 특정 단축키 방식은 **접근성 권한이 필요 없고 App Sandbox·App Store에서도 동작**하므로 배포 전략과 충돌하지 않는다. (임의 키 입력을 가로채는 `CGEventTap`/전역 이벤트 모니터는 접근성 권한이 필요하고 App Store 심사에 불리하므로 **핵심 퀵 캡처에는 사용하지 않는다**.)
- **로컬 알림:** 알림 권한 요청.
- **로그인 시 실행:** Login Items 등록(SMAppService).
- **Dock/메뉴바:** 아이콘·배지·팝오버 제공.

### 8.6 배포 & 공증 (Distribution & Notarization)

> **핵심 원칙 — 첫 릴리스부터 App Sandbox 호환으로 설계한다.** Developer ID 직접 배포는 샌드박스가 필수가 아니지만, 이후 App Store 전환 시 재작업을 피하려면 처음부터 샌드박스에서 통과하는 구현을 택한다: §8.2의 커스텀 URL 스킴 리디렉션, §8.5의 `RegisterEventHotKey`, 네트워크는 `network.client`만 사용 등. 이 원칙을 어기면(예: loopback 서버·`CGEventTap` 의존) App Store 단계에서 인증·핫키 로직을 뜯어고쳐야 한다.

**Phase 1 — Developer ID 직접 배포(공증) · v1.0**
- Apple Developer Program 가입 후 **Developer ID Application** 인증서로 코드 서명.
- **Hardened Runtime 활성화**(공증 필수) + 필요한 최소 엔타이틀먼트만 부여.
- `notarytool`로 Apple 공증 제출 → `stapler`로 티켓 스테이플 → Gatekeeper 경고 없이 실행.
- **DMG**로 배포하고, **Sparkle** 프레임워크로 자동 업데이트 제공(서명된 appcast 피드).
- CI 파이프라인에서 서명·공증·스테이플을 자동화.

**Phase 2 — Mac App Store · 이후**
- **App Sandbox 활성화**(스토어 필수) + 엔타이틀먼트: `com.apple.security.network.client`, `keychain-access-groups` 등 최소 집합.
- 자동 업데이트는 **App Store 기본 메커니즘** 사용(Sparkle 비활성화). 업데이트 계층을 추상화해 빌드 플래그로 전환.
- App Store 심사 가이드라인 준수(수익화 시 IAP 정책 포함).
- **Developer ID 빌드**와 **App Store 빌드** 두 개의 빌드 구성을 병행 유지한다.

### 8.7 인증·세션 무결성 설계 (Multi-Account Auth)
멀티 계정의 세션 만료를 매끄럽게 처리해 "끊김 없는" 경험을 보장한다.

- **토큰 수명주기:** 계정별 액세스 토큰(단기)+리프레시 토큰(장기)을 Keychain에 격리 저장(§8.2). 액세스 토큰은 만료 전 **사전 무음 갱신**한다.
- **하드 만료·철회 감지:** 리프레시가 실패(계정 철회, 비밀번호 변경, 동의 해제, 장기 미사용 등)하면 401을 단순 갱신 필요와 구분해 판정하고, 해당 계정을 **"재로그인 필요"** 상태로 전환한다.
- **부분 만료 격리:** 계정별 세션 상태를 독립 관리해 일부 계정 만료가 **다른 계정·앱 전체를 막지 않게** 한다. 만료 계정의 태스크는 숨기지 않고 상태 라벨로 표시한다.
- **비차단 재인증 흐름:** 전역 모달 대신 해당 계정 배너/배지 → 원클릭 → 브라우저 OAuth(커스텀 URL 스킴 복귀, §8.2) → 토큰 재발급.
- **끊김 없는 복원(리다이렉트):** OAuth 콜백 수신 후 **직전 컨텍스트로 복귀**한다(같은 하루 뷰·스크롤·작성 중 입력 보존). 진행 중이던 변경은 outbox에 남아 재인증 후 플러시된다(§8.3).
- **오류 분류:** 일시적(네트워크)·인증(갱신 후에도 401)·쿼터(429)를 구분해 **인증 실패에만** 재로그인 UX를 트리거한다.

---

## 9. UX / 디자인 원칙

- **작지만 밀도 있게:** 작은 창에 최대한 많은 태스크를 읽기 쉽게. 불필요한 여백·장식 배제.
- **방해 최소화:** 항상 떠 있어도 시선을 뺏지 않도록 저채도·반투명 옵션·자동 페이드.
- **캡처는 즉각적으로:** 퀵 캡처는 열림→입력→저장까지 마찰 0. 마우스 없이 완결.
- **계정은 은은하게 구분:** 색상/아바타로 소속을 알리되 시각적 소음이 되지 않게.
- **네이티브 감성:** macOS 관습(단축키, 컨텍스트 메뉴, 다크모드, 다이내믹 타입)을 따른다.
- **키보드 우선:** 모든 핵심 동작은 단축키로 가능.
- **하루에 집중:** 주 화면은 하루. 넓은 기간은 히트맵으로 조망하되 달력 그리드는 지양한다.
- **끊김 없는 인증:** 세션 만료는 조용히 처리하고, 필요한 계정만 격리해 비차단으로 안내하며, 로그인 후 하던 자리로 되돌린다.
- **언어는 시스템을 따른다:** 한국어·영어를 시스템 언어에 맞춰 표시하고, 날짜·요일 표기를 로케일에 맞춘다.

---

## 10. 릴리스 계획 (마일스톤)

**MVP (v1.0) — P0 항목 · 배포: Developer ID 공증 (§8.6 Phase 1)**
- 항상-위 창 + 위치/크기 복원 (FR-1.1~1.2)
- **하루 중심 뷰 + 좌우 하루 이동 + 기한없음/지난-미완료 처리** (FR-DAY-1~5)
- Google Tasks 태스크/서브태스크/리스트 CRUD + 완료 토글 + 정렬 (FR-2.x P0)
- 멀티 계정 통합 뷰 + 계정 구분 + 필터 (FR-3.x P0)
- **동기화 무결성: 낙관적 UI + 내구성 outbox + 멱등성 + 폴링 증분 + 오프라인** (FR-SYNC-1~7)
- **멀티 계정 세션·인증 무결성: 자동 갱신 + 부분 만료 격리 + 비차단 재로그인 + 상태 복원** (FR-AUTH-1~6)
- Dock 아이콘 표시/포커스 (FR-4.1)
- 전역 단축키 퀵 캡처 + 창 토글 (FR-5.1~5.4)
- **다국어(한/영) + 로케일 인지 날짜 포맷** (FR-I18N-1, FR-I18N-3)
- 로그인 시 자동 실행 (FR-6.6)
- OAuth(PKCE, URL 스킴)+Keychain
- **Developer ID 서명 · `notarytool` 공증 · DMG 배포 · Sparkle 자동 업데이트**
- **(선행 요건) 첫 코드부터 App Sandbox 호환 구조 준수** — 이후 App Store 전환의 전제

**v1.1 — 후속 (P1) · 배포: Developer ID 공증 유지**
- **히트맵(주/월 밀도 + 클릭 이동)** (FR-DAY-6~9)
- 동기화 상태 시각화 (FR-SYNC-8), 재인증 후 큐 플러시 (FR-AUTH-7)
- 다국어 나머지: 앱 내 언어 전환·주 시작 요일·양언어 NLP·번역 확장 대응 (FR-I18N-2,4,5,6)
- 로컬 알림 (FR-6.2), 검색 (FR-6.4)
- 메뉴바 팝오버 + Dock 배지 (FR-4.2~4.4)
- 자연어 마감일 파싱 (FR-5.5), 창 내 키보드 내비 (FR-5.6)
- 투명도/컴팩트 모드, 테마/개인화 (FR-1.3~1.4, FR-6.5)

**App Store 출시 (Phase 2) — 기능 버전과 병행하는 별도 배포 트랙**
- **목표 시점:** v1.1 안정화 + OAuth 검증 완료를 조건으로 **v1.0 공개 후 약 8–12주**. **OAuth 민감 스코프 검증은 개발 초기(비공개 베타)부터 착수**한다(최장 지연 요인이므로 선행).
- App Sandbox 활성화 + 엔타이틀먼트 정리, 업데이트 계층을 스토어 방식으로 전환(Sparkle off)
- OAuth 동의 화면 검증 완료(민감 스코프), App Store 심사 통과
- **수익화 활성화(§11):** StoreKit 인앱 구매(Pro) + Developer ID용 라이선스 키 체계 구축, **v1.0 사용자 Pro 그랜드페더링**
- App Store 빌드 구성 분리 및 릴리스 파이프라인 구축

**v1.2+ — 향후 (P2)**
- 멀티 핀 창, 알림센터 위젯, Focus 연동, 앱 레벨 반복 태스크 등 (FR-6.7~6.10)

---

## 11. 비즈니스 모델 & 수익화 (Freemium)

**모델: Freemium(무료 코어 + 일회성 Pro 잠금 해제).** 자체 서버가 없어(§3.2) 반복 인프라 비용이 없으므로 **구독보다 일회성 구매**를 기본으로 한다. **멀티 계정을 Pro의 앵커**로 삼는다(명확한 파워유저 경계이자 헤드라인 기능).

**무료 (Free)**
- **Google 계정 1개**, 태스크/서브태스크/리스트 전체 CRUD·완료·정렬
- 항상-위 창 + 하루 중심 뷰 + 하루 이동 + Inbox/Overdue
- 전역 퀵 캡처 및 창 토글 단축키
- 한국어·영어, 기본 동기화·오프라인

**Pro (일회성 구매)**
- **멀티 계정(2개 이상)** 통합 뷰
- 히트맵, 로컬 알림, 메뉴바 팝오버 + Dock 배지
- 멀티 핀 창, 위젯, Focus 연동
- 창 투명도·컴팩트·고급 테마
- (향후) 태스크-리소스 링크(파일/URL/앱 바로가기)

**출시 단계별 적용 (확정)**
- **v1.0 (Developer ID) 은 전 기능 무료로 공개**해 사용자·피드백을 모은다.
- **페이월은 App Store 출시 시점(Phase 2)에 활성화**하고, 그 이전 사용자는 **Pro로 그랜드페더링**한다.
- 결제 경로는 빌드별로 갈린다 — **App Store = StoreKit 인앱 구매(비소모성)**, **Developer ID = 웹 체크아웃(예: Paddle 등) + 라이선스 키**. 두 경로 간 권한 복원/이식은 향후 과제.

---

## 12. 성공 지표 (제안)

- **활성화:** 설치 후 1개 이상 Google 계정 연동 완료율.
- **핵심 습관:** 전역 퀵 캡처를 통한 태스크 생성 비율(전체 생성 중).
- **상시성:** 일 활성 세션에서 항상-위 창이 켜져 있는 비율.
- **멀티 계정 채택:** 2개 이상 계정 연동 사용자 비율.
- **리텐션:** D7 / D30 잔존율.
- **성능:** 유휴 상태 평균 메모리/CPU (목표치는 실측 후 확정).

---

## 13. 리스크 & 완화

| 리스크 | 영향 | 완화 |
|---|---|---|
| Tasks 스코프 OAuth 검증 지연 | 배포 일정 지연 | 초기에 검증 절차 착수, 초반엔 테스트 사용자 범위로 운영 |
| API 시간 마감/리마인더 미지원 | 사용자 기대 불일치 | 온보딩에서 "날짜 단위" 명시 + 로컬 알림 제공 |
| 폴링으로 인한 쿼터 초과/지연 | 동기화 지연·오류 | 적응형 폴링 주기 + 지수 백오프, 계정별 조절 |
| 전역 단축키 권한 마찰 | 온보딩 이탈 | `RegisterEventHotKey` 채택으로 접근성 권한 불필요 → 리스크 대부분 해소(§8.5) |
| **App Store 전환 시 재작업** | 일정·비용 증가 | **첫 릴리스부터 샌드박스 호환 설계**(URL 스킴 인증·RegisterEventHotKey·network.client) + 업데이트 계층 추상화(§8.6) |
| 상시 실행 리소스 부담 | 배터리/성능 불만 | 네이티브 경량 스택, 유휴 시 폴링·렌더링 최소화 |
| 계정 간 이동의 재생성 방식 | 데이터 일관성/링크 유실 | 이동을 명시적 동작으로 표시, 실패 시 롤백 |
| `insert` 재시도로 인한 태스크 중복 | 중복 생성 | in-flight 추적 + `updatedMin` 재조정으로 멱등 처리(§8.3 b) |
| 부분 계정 만료가 앱 전체를 차단 | 전면 사용 불가 | 계정별 세션 격리 + 비차단 재로그인(§8.7) |
| 하루 뷰에서 기한없음·지난 미완료 태스크 유실 | 할 일 누락 | 기한없음 전용 영역 + 지난 미완료 이월 정책(FR-DAY-4·5) |
| 동기화 충돌로 데이터 덮어쓰기 | 사용자 편집 손실 | LWW 기본 + etag 조건부(가능 시) + 충돌 상태 표시(§8.3 d, FR-SYNC-8) |

---

## 14. 결정 사항 & 미해결 질문

**결정됨**
- **배포 채널:** Developer ID 직접 배포(공증) 우선 → 이후 Mac App Store (§8.6).
- **전역 핫키 구현:** `RegisterEventHotKey` 기반 — 접근성 권한 불필요, 샌드박스/App Store 호환 (§8.5).
- **최소 지원 OS:** macOS 14 Sonoma, 타깃은 최신 macOS (NFR-5).
- **뷰 모델:** 하루 중심(상단 좌우 이동) + 히트맵 조망, **달력 그리드 미제공** (§6.6).
- **하루 뷰 구성·정렬:** `지남 → 당일 → 기한없음(Inbox)`, 정렬은 계정 → 리스트 → `position`, 완료는 그룹 하단 (§6.6).
- **기한없음/이월:** Inbox 상시 노출 + 지난 미완료는 '오늘'에 **비파괴 Overdue 오버레이** (§6.6).
- **퀵 캡처 기본:** 날짜 없이 Inbox 저장(날짜 파싱 시 해당일), 오늘 저장은 설정 옵션 (§6.6).
- **히트맵:** 밀도 = **미완료 태스크 수**, **월 그리드 기본 + 3개월 토글** (§6.6).
- **폴링:** 적응형 **60초(활성)/5분(백그라운드)/15분(유휴)** + 이벤트 즉시 동기화 + ⌘R 수동 (§8.3 c).
- **충돌 해결:** 필드 단위 `patch` + LWW 기본, **etag 조건부는 스파이크 검증 후 v1.1+** (§8.3 d).
- **인증:** 계정별 세션 격리 + 비차단 재로그인 + 로그인 후 상태 복원 (§8.7).
- **다국어:** 한국어·영어, 시스템 언어 우선 + **앱 내 수동 전환 제공** (§6.9).
- **수익화:** Freemium(무료 단일 계정 코어 + 일회성 Pro). **v1.0 전 기능 무료 공개**, 페이월은 App Store 시점에 활성화 + 기존 사용자 그랜드페더링 (§11).
- **App Store 시점:** v1.1 안정화 + OAuth 검증 완료 조건, **v1.0 공개 후 약 8–12주**; OAuth 민감 스코프 검증은 개발 초기 착수 (§10).

**남은 구체화 항목 (다음 단계)**
1. Pro **가격 포인트(금액)** 및 지역별 가격 — 베타 피드백 후 확정.
2. 프로젝트 킥오프 기준 **실제 일정(주차)** 과 OAuth 검증 착수 D-day.
3. 히트맵 **색 농도 구간(임계값)** 과 오버듀 강조 스타일 — 디자인 단계에서 확정.
4. Tasks API **`If-Match` 지원 스파이크 결과** → 충돌 전략 최종 확정.
5. Developer ID **결제 공급자 선택**(Paddle vs Lemon Squeezy 등)과 두 경로 라이선스 이식 방식.

---

## 부록 A. Google Tasks API (v1) 요약

**서비스 엔드포인트:** `https://tasks.googleapis.com`

**리소스: `tasklists`** — 태스크 리스트 관리
`list`, `get`, `insert`, `patch`, `update`, `delete`

**리소스: `tasks`** — 개별 태스크 관리
`list`, `get`, `insert`, `patch`, `update`, `delete`, `move`, `clear`
- `move`: 같은 리스트 내 위치(`previous`), 계층(`parent`), 다른 리스트로 이동(`destinationTasklist`) 지정
- `clear`: 리스트 내 완료 태스크 일괄 삭제

**핵심 제약 요약**
- 마감일(`due`)은 **날짜 단위**만 저장(시간 무시)
- 서브태스크는 **1단계** 중첩만 지원
- **반복/리마인더/푸시 알림 미지원** → 앱 레벨(로컬 알림·템플릿)로 보완
- 실시간 동기화 없음 → **폴링** 기반
- 사용량 제한(쿼터) 존재

*출처: Google Tasks API Reference (developers.google.com/workspace/tasks/reference/rest), 최종 업데이트 2025-03-13.*
