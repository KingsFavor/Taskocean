<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Developer ID direct-distribution track (docs/release.md Track B).
	     Used by CI (.github/workflows/release.yml) for notarized DMG + Homebrew. -->
	<key>method</key>
	<string>developer-id</string>
	<key>teamID</key>
	<string>4S9VPFZ465</string>
	<!-- Manual signing so CI signs with the imported Developer ID Application cert
	     instead of trying to talk to an interactive signing session. -->
	<key>signingStyle</key>
	<string>manual</string>
	<key>signingCertificate</key>
	<string>Developer ID Application</string>
	<!-- Secure timestamp is required for notarization. -->
	<key>generateAppStoreInformation</key>
	<false/>
	<key>stripSwiftSymbols</key>
	<true/>
</dict>
</plist>
